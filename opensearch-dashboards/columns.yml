global:
  default_columns:
    - source_type
    - host_ip
    - host_name
    - dlq.orig_index
    - dlq.summary
    - dlq.error_field

types:
  evtxjson:
    # Columns listed here become the initial set of columns in Discover
    # for evtxjson-*.
    default_columns: []

  dissect-evtxjson:
    # Columns listed here become the initial set of columns in Discover
    # for dissect-evtxjson-*.
    default_columns: []

  hayabusajson:
    # Columns listed here become the initial set of columns in Discover
    # for hayabusajson-*.
    default_columns: []

  mftjson:
    # Columns listed here become the initial set of columns in Discover
    # for mftjson-*.
    default_columns: []

  plasojson:
    # Columns listed here become the initial set of columns in Discover
    # for plasojson-*.
    default_columns: []

  json-arrayjson:
    # Columns listed here become the initial set of columns in Discover
    # for json-arrayjson-*.
    default_columns: []

  json-passthroughjson:
    # Columns listed here become the initial set of columns in Discover
    # for json-passthroughjson-*.
    default_columns: []

  nginxjson:
    # Columns listed here become the initial set of columns in Discover
    # for nginxjson-*.
    default_columns: []

  cloudtrailjson:
    # Columns listed here become the initial set of columns in Discover
    # for cloudtrailjson-*.
    default_columns: []

  csvjson:
    # Columns listed here become the initial set of columns in Discover
    # for csvjson-*.
    default_columns: []

  csv-headerlessjson:
    # Columns listed here become the initial set of columns in Discover
    # for csv-headerlessjson-*.
    default_columns: []

